<?xml version="1.0" encoding="utf-8"?>

<layout>

    <data>
        <import type="teamideals.com.trackitez.User"/>
        <variable name="user" type="teamideals.com.trackitez.User"/>
    </data>

    <android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/content_main_parent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context=".MainActivity"
        tools:showIn="@layout/app_bar_main">

        <!--
            Data binding invokes the getfName method of user instance bound to
            this layout and attaches it to the text attribute of the text view
        -->
        <TextView
            android:id="@+id/textView2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{user.getfName()}" />

    </android.support.constraint.ConstraintLayout>

</layout>